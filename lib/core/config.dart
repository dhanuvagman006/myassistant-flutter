/// Central compile-time configuration.
/// No secrets live here — all credentials are stored server-side.
library config;

// Intentionally empty: avatar credentials (HeyGen) are
// provisioned at runtime by the backend /avatar/session endpoint.
// Add any future compile-time feature flags here.
